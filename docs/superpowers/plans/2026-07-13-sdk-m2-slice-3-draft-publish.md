# SDK-M2 Slice 3 — Draft → Review & publish

**Goal:** Chat → draft topic → review modal → publish → topic appears in All list.

**Routes:** `POST /roundtable/v1/case`, `POST /roundtable/v1/publish`

**UI:** "Turn into topic" → outcome card → Review & publish modal → list refresh.

**Deferred:** Started tab, dedupe (`/cases/similar`), GitHub escalation.